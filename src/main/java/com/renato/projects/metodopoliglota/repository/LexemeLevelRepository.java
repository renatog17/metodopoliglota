package com.renato.projects.metodopoliglota.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.renato.projects.metodopoliglota.domain.LexemeLevel;

public interface LexemeLevelRepository extends JpaRepository<LexemeLevel, Long>{

}
